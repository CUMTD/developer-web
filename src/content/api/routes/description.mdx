# Routes

All bus routes belong to a larger "route group." The route group dictates universal properties like color, foreground text color and sort order for the routes it contains.

```mermaid
 graph TD;
          A-->B;
          A-->C;
          B-->D;
          C-->D;

```
