export default function ReferencePage() {
	return (
		<div className="max-w-[80em]">
			<h1>Getting Started</h1>
			<p>
				The MTD API is organized around REST. API has predictable resource-oriented URLs, accepts form-encoded request
				bodies, returns JSON-encoded responses, and uses standard HTTP response codes, authentication, and verbs.
			</p>
		</div>
	);
}
